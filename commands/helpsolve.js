module.exports = function (params, callback) {
    var text = '';

    text += 'an / anagramma - анаграмма';
    text += '\nchlen - расчлененки';
    text += '\nцезарь - шифр цезаря';
    text += '\nmask - поиск по маске';

    callback(text);
};